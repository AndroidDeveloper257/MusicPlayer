package uz.mobiler.musicplayer.utils

enum class NavigationOptions(val navigationName: String) {
    HOME("home"),
    FAVORITE("favorite"),
    RECENT("recent"),
    PLAYLIST("playlist"),
    SEARCH("search")
}